name: Targets Unsupported

on:
  workflow_dispatch:

jobs:
  direct-protocol:
    runs-on: ubuntu-latest
    steps:
      - uses: docker://alpine:3.20

  remote-composite:
    runs-on: ubuntu-latest
    steps:
      - uses: __FIXTURES_REPOSITORY__/composite-actions/docker-unsupported@8f68d9f5083d7de4eba454814778038f7ed72f6e
