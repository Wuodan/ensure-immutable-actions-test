name: Targets Local Reusable Workflow Immutable Child

on:
  workflow_call:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/local-reusable-workflow-immutable-wrapper
