name: Targets Mixed First Party

on:
  workflow_dispatch:

jobs:
  mixed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - uses: __FIXTURES_REPOSITORY__/composite-actions/parent@upstream-PR/fix-workflow-local-action-path-resolution
      - uses: __FIXTURES_REPOSITORY__/composite-actions/leaf@8f68d9f5083d7de4eba454814778038f7ed72f6e
