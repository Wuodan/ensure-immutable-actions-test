name: Targets Immutable

on:
  workflow_dispatch:

jobs:
  remote-composite:
    runs-on: ubuntu-latest
    steps:
      - uses: __FIXTURES_REPOSITORY__/composite-actions/parent@8f68d9f5083d7de4eba454814778038f7ed72f6e

  remote-reusable:
    uses: __FIXTURES_REPOSITORY__/.github/workflows/reusable-immutable.yml@8f68d9f5083d7de4eba454814778038f7ed72f6e

  local-reusable-workflow:
    uses: ./.github/workflows/targets-local-reusable-workflow-immutable-parent.yml
