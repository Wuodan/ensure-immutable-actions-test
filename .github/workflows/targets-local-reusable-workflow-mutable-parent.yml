name: Targets Local Reusable Workflow Mutable Parent

on:
  workflow_call:

permissions:
  contents: read

jobs:
  child:
    uses: ./.github/workflows/targets-local-reusable-workflow-mutable-child.yml
