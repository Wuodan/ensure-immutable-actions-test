name: Targets Mutable

on:
  workflow_dispatch:

jobs:
  remote-composite:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: Wuodan/ensure-immutable-actions-test-custom-actions/composite-actions/parent@upstream-PR/feat-scan-nested-actions-in-first-party-reusable-workflows

  remote-reusable:
    uses: Wuodan/ensure-immutable-actions-test-custom-actions/.github/workflows/reusable.yml@upstream-PR/feat-scan-nested-actions-in-first-party-reusable-workflows
