name: Targets Mutable

on:
  workflow_dispatch:

jobs:
  remote-composite:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: Wuodan/ensure-immutable-actions-test-fixtures/composite-actions/parent@__FIXTURES_BRANCH__

  remote-composite-duplicate:
    runs-on: ubuntu-latest
    steps:
      - uses: Wuodan/ensure-immutable-actions-test-fixtures/composite-actions/parent@__FIXTURES_BRANCH__

  remote-reusable:
    uses: Wuodan/ensure-immutable-actions-test-fixtures/.github/workflows/reusable.yml@__FIXTURES_BRANCH__

  local-reusable-workflow:
    uses: ./.github/workflows/targets-local-reusable-workflow-mutable-parent.yml
