name: Targets Local Reusable Workflow Immutable Child

on:
  workflow_call:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: Wuodan/ensure-immutable-actions-test-fixtures/composite-actions/local-reusable-workflow-target@63b1cf61055a58adbce3e8b7c638d6070e1eb544