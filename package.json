{
  "name": "ensure-immutable-actions-test",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "format:check": "prettier --check .",
    "format:write": "prettier --write .",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.39.2",
    "prettier": "^3.8.3"
  }
}
