{
  "name": "ensure-immutable-actions-test",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "format:check": "prettier --check .",
    "format:write": "prettier --write .",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^10.2.1",
    "prettier": "^3.8.3"
  }
}
